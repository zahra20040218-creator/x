node_modules/
dist/
build/
coverage/
.env
.env.local
*.log
logs/*.log
.dart_tool/
.flutter-plugins*
.idea/
.vscode/
*.iml
.DS_Store
pnpm-debug.log*
.turbo/

# Database dumps. They contain phone numbers (PII, CLAUDE.md §9) and must
# never reach source control.
backups/
*.dump
*.dump.sha256

# Redis persistence artifact. `redis-server` writes it into whatever directory
# it was started from, which during local testing is the repository root. It is
# a database snapshot, not source.
dump.rdb
