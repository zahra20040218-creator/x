# ما تم إنجازه — بلغة بسيطة

هذا الملف يشرح ما بُني، سطراً بسطر، بلا مصطلحات تقنية.

---

## اقرأ هذا السطر أولاً

**المنتج مبني بالكامل الآن:** خادم يعمل، تطبيق راكب، تطبيق سائق، ولوحة إدارة.

لكنه **لم يُتحقَّق منه**: قاعدة البيانات لم تُشغَّل ولا مرة، والتطبيقان لم يُصرَّفا
ولا مرة، وأخطر ميزة فيه (منع تضارب السائقين) أثبتها حَكَمٌ صنعتُه أنا ولم يتحقق
منه أحد.

الفرق مهم: ما يمنع الإطلاق اليوم ليس نقصاً في البناء، بل **نقصاً في التحقق**.

التفصيل الكامل في `FINAL_REPORT.md`.

---

## ما بُني فعلاً

### 1. الأساس والعقود

كُتبت "خرائط" المشروع الثلاث: شكل قاعدة البيانات (`docs/schema.sql`)، وقائمة كل
الطلبات المسموح بها بين التطبيق والخادم (`docs/api-contract.yaml`)، وخريطة مراحل
الرحلة (`docs/state-machine.md`). هذه الخرائط تمنع لاحقاً أن يخترع أحد ميزة على
هواه خارج المتفق عليه.

كُتبت أيضاً ملفات التشغيل الذاتي التي طلبتها: `Dockerfile` و `run.sh` و
`AGENT_LOOP_PROMPT.md`، جاهزة كما وصفها ملفك.

### 2. المال — لا كسور عشرية أبداً

بُني نوع خاص للمبالغ لا يقبل إلا **ديناراً صحيحاً**. أي محاولة لإدخال مبلغ فيه
كسر عشري تُرفض فوراً وتوقف العملية بدل أن تمر بصمت.

لماذا هذا مهم: في الحسابات العادية، `1100 × 7%` تساوي `77.00000000000001` وليس
`77`. الفرق غير مرئي على الشاشة، لكنه يتراكم عبر آلاف الرحلات حتى يظهر كخلاف مع
سائق. اختبرنا هذا على آلاف القيم: الطريقة القديمة تُنتج كسوراً في مئات الحالات،
وطريقتنا لا تُنتج ولا واحدة.

### 3. دفتر الحسابات — القيد المزدوج

كل عملية مالية تُسجَّل بطريقة المحاسبة المزدوجة: كل مبلغ يُكتب مرتين، مرة خصماً
ومرة إضافة، والمجموع صفر دائماً. إن لم يكن المجموع صفراً، **يرفض النظام الكتابة
أصلاً**، وقاعدة البيانات ترفضها مرة ثانية كطبقة حماية إضافية.

الدفتر **لا يُعدَّل ولا يُحذف منه أبداً**. أي تصحيح يُكتب كقيد جديد معاكس، فيبقى
الأصل والتصحيح كلاهما مرئيين. لا توجد في الكود دالة تعديل أو حذف — ليست موجودة
ومعطّلة، بل غير موجودة.

رصيد محفظة السائق **يُحسَب** من الدفتر في كل مرة، ولا يُخزَّن كرقم منفصل. هذا يمنع
أشهر خلل في المحافظ: أن يختلف الرصيد المخزَّن عن مجموع الحركات.

العمولة **صفر** كما طلبت، وتُغيَّر من الإعدادات بلا حاجة لإعادة نشر البرنامج.

### 4. مراحل الرحلة — لا قفزات غير مسموحة

بُني "حارس" يحكم انتقال الرحلة بين مراحلها. جرّبنا **كل** التركيبات الممكنة
(١٢١ تركيبة): المسموح منها ١٦ فقط، وكل ما عداها يُرفض برسالة خطأ واضحة، ولا
يُتجاهَل بصمت.

الأهم: **صلاحية المستخدم جزء من الانتقال نفسه**، لا فحص منفصل بجانبه. أي أن السائق
"أ" لا يستطيع إنهاء رحلة السائق "ب" — ليس لأن هناك فحصاً قد يُنسى، بل لأن القاعدة
نفسها لا تسمح بذلك.

### 5. منع تضارب السائقين

بُنيت آلية "الحجز الذرّي": عندما يضغط عدة سائقين "قبول" في اللحظة نفسها، سائق واحد
فقط يفوز، والباقون يحصلون على رسالة "الرحلة لم تعد متاحة".

اختبرناها بـ **٥٠ سائقاً يضغطون في اللحظة نفسها**: فاز واحد فقط، في كل مرة.
وأضفنا خط دفاع ثانٍ في قاعدة البيانات يجعل وجود رحلتين نشطتين لسائق واحد مستحيلاً
حتى لو تعطّل الخط الأول.

⚠️ **تحفّظ مهم:** هذا الاختبار جرى على نسخة *محاكية* من برنامج Redis، لأن Redis
الحقيقي غير مثبَّت على هذا الجهاز. اقرأ `FINAL_REPORT.md` — هذه أهم نقطة في التقرير.

### 6. منع تكرار الرحلات عند انقطاع الشبكة

عندما تنقطع شبكة الراكب ويعيد التطبيق المحاولة، يتعرّف الخادم على أنه الطلب نفسه
ويعيد **الرحلة الأصلية** بدل إنشاء رحلة جديدة. جرّبنا الضغط ٥ مرات متتالية:
أُنشئت رحلة واحدة فقط.

### 7. أرقام الهواتف

يقبل النظام كل الأشكال التي قد يكتبها شخص حقيقي: `07701234567` و `+9647701234567`
و بمسافات أو شرطات، وحتى **بالأرقام العربية** `٠٧٧٠١٢٣٤٥٦٧`. كلها تُحوَّل إلى شكل
واحد موحّد.

لماذا هذا مهم: لو لم تُوحَّد، لأنشأ الشخص نفسه حسابين بالخطأ وضاع سجل رحلاته.

### 8. حماية الخصوصية في السجلات

سجلات النظام **تحذف أرقام الهواتف والأسماء تلقائياً**، مهما كان عمقها داخل
البيانات. والمواقع الجغرافية تُقرَّب إلى حدود ١١٠ متر — يكفي لتشخيص مشكلة، ولا يكفي
لمعرفة بيت أحد.

### 9. خلل خطير اكتُشف وأُصلح

أثناء المراجعة العدائية اكتُشف خلل حقيقي: وظيفة تنظيف الطلبات القديمة كانت قد
تحذف — بالخطأ — مفتاح راكب آخر ما زال نشطاً، مما يسمح بإنشاء **رحلة مكرَّرة**.

أُصلح، وكُتب اختبار يمنع عودته. وتأكدنا أن الاختبار حقيقي: أعدنا الخلل عمداً فسقط
الاختبار، ثم أصلحناه فنجح.

### 10. دورة حياة الرحلة كاملة (منطقياً)

رُبطت القطع السابقة معاً: إنشاء الرحلة → عرضها على أقرب سائق → قبولها → الوصول →
بدء الرحلة → إنهاؤها وتسوية المال — كلها في خدمة واحدة مختبَرة.

الأهم أن **تسوية المال تحدث كلها معاً أو لا تحدث إطلاقاً**: تغيير حالة الرحلة،
وتسجيل الأجرة، وإنشاء سجل الدفع، وكتابة قيود الدفتر. اختبرنا هذا بإفشال كتابة
الدفتر عمداً: لم يُكتب أي شيء، وبقيت الرحلة كما كانت.

واختُبر التضارب هنا من طرف إلى طرف: **٢٠ سائقاً يقبلون في اللحظة نفسها → واحد فقط
ينجح**، والبقية يحصلون على "الرحلة لم تعد متاحة".

### 11. توزيع الرحلات على السائقين

بُني نظام يعرف أين السائقون (في Redis فقط، كما يوجب الملف) ويعرض الرحلة على **أقرب
سائق مؤهَّل، واحداً تلو الآخر مع مهلة** — لا على الجميع دفعة واحدة، لأن العرض
الجماعي هو ما يُنتج تسابق سائقين على راكب واحد.

السائق الموقوف أو المشغول لا يصله عرض إطلاقاً، وهذا الفحص يقرأ من قاعدة البيانات لا
من الذاكرة المؤقتة، حتى لا يعود سائق موقوف "مؤهَّلاً" لمجرد أن الذاكرة أُفرغت.

وإذا لم يردّ السائق خلال المهلة، تنتقل الرحلة تلقائياً للمرشّح التالي، ولا تبقى
عالقة أبداً.

### 12. تثبيت نسبة العمولة لحظة الطلب

نسبة العمولة تُجمَّد على الرحلة لحظة إنشائها. لو غيّرتَ النسبة والسائق في منتصف
الرحلة، **تُحسَب أجرته بالنسبة التي وافق عليها**، لا بالجديدة. اختبرنا هذا تحديداً.

### 13. الدفع والاسترجاع

بُنيت واجهة الدفع كما طلب الملف: الدفع نقداً مُنفَّذ بالكامل، وبوابة الدفع
الإلكتروني **هيكل فارغ يرفض العمل صراحةً** (لا كود ميت يوهم بأنه يعمل).

ومعالج إشعارات البوابة كُتب كدالة نقية واختُبر **الآن** كما يوجب الملف — لأن البوابة
المستهدفة لا تسمح بالاختبار قبل الإطلاق. فحصنا فيه الحالات الخبيثة: توقيع مزوَّر،
توقيع ناقص، مبلغ بكسر عشري، ومبلغ عمولة أكبر من الأجرة. كلها مرفوضة.

---

## بالأرقام

| البند | العدد |
|---|---|
| اختبارات آلية ناجحة | **٦٥٣** (٦٤٢ خادم + ١١ لوحة) |
| اختبارات فاشلة | **صفر** |
| أخطاء في فحص الأنواع | **صفر** |
| أخطاء في فحص جودة الكود | **صفر** |
| مهام مكتملة ومختبَرة | ٣٢ من ٤٤ |
| مهام متوقفة لغياب أدوات | ١٢ |
| مهام لم تُبدأ بعد | **صفر** |

**لم يُحذف أي اختبار ولم يُضعَّف أي اختبار لجعل النتيجة خضراء.**

---

## جلسة التدقيق — ما تغيّر

### تدقيق كامل قبل أي تعديل

فُحص المستودع من الصفر بلا اعتماد على أي تقرير سابق. النتيجة في
`CURRENT_STATE_REPORT.md`، وأهم ما فيها:

**الخادم يعمل فعلاً.** لم يُكتفَ بالاختبارات: شُغّل الملف المبني، وربط ٣٦ مساراً،
واستُقبلت طلبات HTTP حقيقية وردّ عليها. وبلا قاعدة بيانات ولا Redis، **بدأ ولم
ينهَر** — يردّ على فحص الحياة بـ200، ويرفض غير المصرّح لهم بـ401، ويقول عن جاهزيته
«معطّلة» بـ503. هذا هو التدهور الرشيق المطلوب، مُثبَتاً لا مُدّعىً.

**لا توجد `TODO` ولا كود وهمي** في مسار الإنتاج. الـ١٢ نتيجة التي ظهرت كانت كلها
`toDouble()` وليست مهام معلّقة.

### 19. حد لعدد الطلبات

كان بإمكان أي شخص استدعاء نقطة تسجيل الدخول بحلقة لا نهائية — وكل استدعاء يكلّف
تحققاً حقيقياً من Firebase، أي **فاتورة**، وأيضاً وسيلة لاستكشاف من يملك حساباً.

الآن: ١٠ محاولات في الدقيقة لكل عنوان. والحد يُحسب **لكل مستخدم** لا لكل عنوان
بعد تسجيل الدخول — لأن شبكة اتصالات واحدة في بغداد تضع آلاف المستخدمين خلف عنوان
واحد، وكان ذلك سيجعلهم يستنزفون حصص بعضهم.

⚠️ **بصراحة:** بلا Redis يعمل هذا الحد بـ«الفشل المفتوح» — أي يسمح بالمرور ويسجّل
تحذيراً. تحققتُ من ذلك في الملف المبني فعلاً: ١٣ طلباً أنتجت ١٣ تحذيراً. أي أن
**الحد غير فعّال في هذه البيئة** حتى يُشغَّل Redis.

### 20. سجل تدقيق لأعمال الإدارة

لم يكن هناك أي أثر لمن فعل ماذا. الآن كل عمل إداري حساس يُسجَّل: من، وماذا، وعلى
من، ومتى، وهل نجح.

الأهم أن هذا يشمل **المال**: من شحن محفظة سائق بكم، ومن غيّر نسبة العمولة ومن أي
قيمة إلى أي قيمة. والسجل **لا يُعدَّل ولا يُحذف منه** كالدفتر تماماً.

وفيه تفصيلان مقصودان: يُسجَّل **الفشل** أيضاً لا النجاح فقط — لأن أول سؤال عند أي
حادثة هو «هل حاول أحد ورُفض؟». وأرقام الهواتف والأسماء **تُحذف قبل الكتابة في
قاعدة البيانات**، لا في السجلات فقط، لأن هذا السجل يُصدَّر عند النزاعات.

### ثلاثة أخطاء ارتكبتُها أنا واكتُشفت أثناء التحقق

- معالج جديد في قاعدة بيانات الاختبار **حجب** معالجاً قديماً، فتوقف تحرير السائق
  بعد انتهاء الرحلة. نفس نمط الخطأ الذي اكتُشف سابقاً
- اختبار عدد ملفات القاعدة رصد الملف الجديد بشكل صحيح
- اختبار الخصوصية كان يفحص وجود «964» كنص — وهو يظهر صدفةً في المعرّفات
  العشوائية. أُصلح ليفحص **نمط رقم هاتف** لا مجرد ثلاثة أرقام

وخطأ رابع في **منهجية اختباري** لا في الكود: أول محاولة لإثبات حد الطلبات فشلت
لأن نسخة قديمة من الخادم كانت ما زالت تشغل المنفذ. أعدتُ التجربة على منفذ نظيف.

---

## ما أُضيف بعد ذلك

### 14. الخادم صار يعمل فعلاً

بُنيت طبقة الاتصال كاملة: تسجيل دخول برقم الهاتف، وكل نقاط الاتصال التي يحتاجها
التطبيقان ولوحة الإدارة. **٢٦ اختباراً يمرّ عبر HTTP حقيقي** من تسجيل الدخول حتى
تسوية المال، ومنها المسارات الثلاثة الفاشلة التي يطلبها ملفك: انقطاع الشبكة،
ورفض السائق، وعدم وجود سائقين.

ومن بين هذه الاختبارات، ثلاثة تجيب مباشرة على أسئلة قائمتك:
- سائقان يضغطان "قبول" معاً → واحد فقط ينجح، والثاني يرى "لم تعد متاحة"
- راكب يحاول قراءة رحلة راكب آخر → «غير موجودة»، لا «ممنوع» (حتى لا يعرف أنها موجودة أصلاً)
- **رقم هاتف الراكب لا يظهر في رد الخادم للسائق إطلاقاً** — لأنه لا يوجد حقل له

### 15. حماية الجلسات

رمز التجديد **يُخزَّن مُشفَّراً لا كنص**، ويُبدَّل عند كل استخدام. لو حاول أحد إعادة
استعمال رمز قديم، يُرفض — وهذا بحد ذاته دليل على سرقته.

وتسجيل دخول السائق **يرفض إنشاء حساب**: الحسابات تنشئها أنت فقط، كما طلبت.

### 16. التطبيقان

تطبيق الراكب وتطبيق السائق، بالعربية ومن اليمين لليسار، وكل نص فيهما يمرّ بطبقة
ترجمة (لا نص مكتوب مباشرة في الشاشة).

في تطبيق السائق بُني **الموقع في الخلفية** بكل ما يطلبه ملفك: خدمة أمامية بإشعار
دائم، وشاشة تشرح للسائق **لماذا** يحتاج استثناء البطارية قبل أن يطلبه منه، وتخزين
مؤقت للمواقع عند انقطاع الشبكة يُرسَل عند عودتها.

⚠️ **لم يُصرَّف أي من التطبيقين ولم يُختبَر.** لا يوجد Flutter على هذا الجهاز.

### 17. لوحة الإدارة

تعرض السائقين والرحلات والنزاعات، وتشحن المحافظ، وتغيّر الأجرة والعمولة.

وفيها تفصيل مقصود: لو أرسل الخادم يوماً مبلغاً بكسر عشري، **تعرضه اللوحة بعلامة
تحذير صارخة بدل أن تقرّبه**. لأن الفحص رقم ٦ في قائمتك يطلب منك النظر في اللوحة
والتأكد من عدم وجود كسور — ولوحة تُقرّب بصمت تجعل ذلك الفحص ينجح دائماً بلا معنى.

### 18. ثلاثة عيوب خطيرة إضافية اكتُشفت وأُصلحت

- سائق لم يردّ على عرض كان يبقى يرى عرضاً وهمياً لرحلة انتقلت لغيره
- سائق لم يكن يستطيع رؤية الرحلة المعروضة عليه قبل أن يقرّر قبولها
- أرقام هواتف عراقية حقيقية محتملة في ملفات الاختبار (يمنعها ملفك صراحةً)

وأثناء إصلاح الأخير اكتُشف أن أحد الاختبارات **صار بلا معنى** — يفحص وجود رقم لم
يعد موجوداً أصلاً — فأُعيدت كتابته.
