<?xml version="1.0" encoding="utf-8"?>
<!--
  CLAUDE.md §5.3. This file is where background location actually lives or dies.

  A missing permission here does not fail loudly - the service simply stops
  reporting a few minutes after the screen goes off, which is indistinguishable
  from "the driver stopped driving". That is why every entry below carries the
  reason it exists.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground location, for while the app is open. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
      "Allow all the time". Without this the position stream stops the moment
      the app leaves the foreground, and the rider watches a stationary car.
      Requested SECOND, after foreground is granted - asking first is
      auto-denied on Android 11+ with no dialog shown at all.
    -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- The foreground service itself, plus its typed variant (Android 14+). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!--
      Doze exemption. CLAUDE.md §5.3 requires requesting it at onboarding with
      an explanatory screen; this permission is what makes the request possible.
      Play Store review asks for a justification for this one - the honest
      answer is that a ride-hailing driver app cannot function without it.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
      Keeps the CPU and wifi radio alive between position fixes. Without them
      the device sleeps between samples and the 5-second interval silently
      becomes minutes.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- The persistent notification the foreground service shows. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Push offers (FCM). -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <!-- Deep-link to Google Maps for navigation (CLAUDE.md §2). -->
    <queries>
        <package android:name="com.google.android.apps.maps" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>

        <!--
          Required by the Flutter engine's ProcessTextPlugin. It was present in
          the rider manifest and absent from the driver's; the merged app is one
          binary and needs both, so dropping it here would have been a silent
          regression for Rider mode.
        -->
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          The location foreground service.

          foregroundServiceType="location" is MANDATORY from Android 14. Without
          it the service is killed at startup with a SecurityException, and the
          app looks like it simply refuses to go online.
        -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location"
            android:exported="false"
            tools:replace="android:foregroundServiceType" />

        <!--
          Restarts the service after a reboot. A driver whose phone restarted
          mid-shift would otherwise appear offline until they noticed and
          reopened the app.
        -->
        <receiver
            android:name="com.pravera.flutter_foreground_task.service.RestartReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Google Maps API key is injected at build time, never committed. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
    </application>
</manifest>
